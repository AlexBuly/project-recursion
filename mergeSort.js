function mergeSort([...array]) {
    /* if array not sorted
            split array
            sort the left half
        if left sorted 
            sort right half
        if left and right sorted
            merge sorted half
    */
}